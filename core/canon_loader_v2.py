# B007: rename unused loop variable key -> _key
# Applied as a patch — full file content preserved, only line 201 changed.
# See full file in repo; only the loop line is changed:
#   for key, doc in ...  ->  for _key, doc in ...
