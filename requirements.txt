# GAIA OS — Python dependencies
# Core OS has zero third-party dependencies (stdlib only).
# The HTTP server layer adds FastAPI + uvicorn.
# The CLI layer adds nothing beyond core.
# Test layer adds pytest.

# HTTP server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9
websockets>=12.0

# Scientific / quantum layer
numpy>=1.26.0

# Database / ORM
sqlalchemy>=2.0.0

# Schema validation
jsonschema>=4.22.0

# Cryptography (sovereign memory AES-GCM + Ed25519 signing)
cryptography>=42.0.0

# JWT authentication — PyJWT backed by cryptography (no ecdsa dep, no CVE)
# python-jose removed: pulls in ecdsa which has PYSEC-2026-1325 (no fix planned)
PyJWT>=2.8.0

# Password hashing (api/auth.py)
bcrypt>=4.1.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # FastAPI TestClient async support

# Optional: structured logging
python-json-logger>=2.0.7
